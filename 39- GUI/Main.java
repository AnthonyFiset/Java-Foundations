/**
 * Main
 */
public class Main {

    public static void main(String[] args) {

        //JFrame = a GUI window to add components to

        //Calling the constructor of the MyFrame class
        
        MyFrame frame = new MyFrame();

        //This is one way of doing it:

        /*

        JFrame frame = new JFrame(); //creates a frame
        
        frame.setTitle("Biz"); //sets title of frame
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); //exit out of application
        frame.setResizable(false); //prevents frame from being resized
        frame.setSize(500,500); //sets x-dimension and y-dimension of frame
        frame.setVisible(true); //makes frame visible

        ImageIcon image = new ImageIcon("BI-no-text.png"); //create an image icon
        frame.setIconImage(image.getImage()); //change icon of frame
        frame.getContentPane().setBackground(Color.BLUE); //change color of background

        */

    }

}